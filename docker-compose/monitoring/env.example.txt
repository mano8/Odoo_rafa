GRAFANA_ADMIN_PASSWORD=changethis

# LAN IP of the host machine (used by Grafana ROOT_URL)
HOST_IP=192.168.1.146

# Management dummy interface IP (docker0-mgmt) — hw_proxy binds here.
# Change only if your management interface uses a different IP.
# Must match: services/odoo_server/hw_proxy.service --host value
MGMT_IP=10.254.254.1
